var dgram = require('dgram');
var message = new Buffer("test:1|c");
var client = dgram.createSocket("udp4");
client.send(message, 0, message.length, 8125, "localhost", function(err, bytes) {
  client.close();
});